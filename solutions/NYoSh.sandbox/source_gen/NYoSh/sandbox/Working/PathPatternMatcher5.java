package NYoSh.sandbox.Working;

/*Generated by MPS */

import com.esotericsoftware.wildcard.Paths;

public class PathPatternMatcher5 {
  /*package*/ String pattern;
  /*package*/ String workingDir;

  public PathPatternMatcher5(String pattern) {
    this.pattern = pattern;
    workingDir = System.getProperty("user.dir");
  }

  public String resolve() {
    Paths paths = new Paths();
    StringBuilder buffer = new StringBuilder();
    // <node> 
    // <node> 
    for (String path : paths.glob(workingDir, pattern).getPaths()) {
      // <node> 
      buffer.append(path);
      buffer.append(" ");
    }
    return buffer.toString();
  }


}
